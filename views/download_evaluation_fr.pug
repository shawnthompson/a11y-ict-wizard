doctype html
html(lang='fr')
  head
    title la liste des exigences en matière daccessibilité des TIC pouvant être testées
    meta(charset='utf-8')
    if format == 'docx'
      style
        include download_docx.css
    else
      style
        include download_html.css
  body(vocab='http://schema.org/' typeof='WebPage')
    .WordSection1
      p Généré le #{moment().format('YYYY-MM-DD')}
      include includes/evaluation_intro_fr.html
      include includes/list_evaluation_fr
      include includes/scope_fr